sub loadProp
#load this property.
{
    my ($self) = @_;

    printf STDERR "LOADING EC PROPERTY\n" if ($DEBUG);

    return 0;
}

sub dumpProp
#dump this property.
{
    my ($self) = @_;

    printf STDERR "DUMPING EC PROPERTY\n" if ($DEBUG);

    return 0;
}

sub loadProps
#load EC property set from database
{
    my ($self) = @_;

    printf STDERR "LOADING EC PROPERTY SET\n" if ($DEBUG);

    return 0;
}

sub dumpProps
#dump EC property set
{
    my ($self) = @_;

    printf STDERR "DUMPING EC PROPERTY SET\n" if ($DEBUG);

    return 0;
}
